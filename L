Field,Bits,Description
Mod,7-6,11 means Register-to-Register mode.
Reg,5-3,The Source register ID.
R/M,2-0,The Destination register ID.


# Constants and Meta-Programming
SCREEN_WIDTH = 80
buffer = allocate(1024)

# Modern Block Syntax
fn main():
    rax = 100                 # Auto-detects mov_imm
    rbx = rax                 # Auto-detects mov_reg

    if rax == rbx:            # Auto-generates cmp + jne/je logic
        rcx = rax + 5         # Auto-generates add

    call print_char('A')      # Auto-handles stack/calling convention



    Instruction,Hex Pattern,Description
"xor rdx, rdx",48 31 D2,Clear rdx (Required before DIV!)
"mov rbx, 10",48 C7 C3 0A 00 00 00,Our divisor
div rbx,48 F7 FB,"Divide rax by rbx. Result in rax, Remainder in rdx."
"add dl, 0x30",80 C2 30,Convert remainder to ASCII character